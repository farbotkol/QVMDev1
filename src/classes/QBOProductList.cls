//
// Generated by JSON2Apex http://json2apex.herokuapp.com/
//

public class QBOProductList {
    
    private static QBOProductList instance = null;// used to hold the singleton response

    public QBOProductList()
    {
        
    }

	public class SalesTaxCodeRef {
		public String value;
		public String name;
	}

	public class Item {
		public SalesTaxCodeRef SalesTaxCodeRef;
		public Boolean sparse;
		public String Id;
        public String Sku;
	}

	public QueryResponse QueryResponse;


	public class QueryResponse {
		public List<Item> Item;
		public Integer startPosition;
		public Integer maxResults;
	}

	
	public static QBOProductList parse(String json) {
		return (QBOProductList) System.JSON.deserialize(json, QBOProductList.class);
	}
    
    public static QBOProductList getInstance(){
        // lazy load only initialize if it doesn't already exist
        if(instance == null)
        {
            QBOCRUDHelper crudHelper = new QBOCRUDHelper();
            
            //instance = new AccountFooRecordType();	
            
            Object res =  crudHelper.query('SELECT * FROM ITEM WHERE Active = true and Sku != \' \'', QBOProductList.Class); 
        
            if (res instanceof QBOError)
            {
                System.debug('error : '  + (QBOError)res); 
                instance  =  null;
            }
            else 
            {
                System.debug('Yah!! : '  + (QBOProductList)res); 
                instance =  ((QBOProductList)res);
            }      
            
        }
        return instance;
    }

}